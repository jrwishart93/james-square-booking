"use client";
import { useEffect } from "react";

export default function AgreementClient() {
  useEffect(() => {
    // signature pads / localStorage / DOM setup
  }, []);

  return (
    <div id="agreement-root">
      {/* your interactive agreement JSX */}
    </div>
  );
}
